import React from "react";
import { Box, Typography } from "@mui/material";

const SettingsTab = () => {
  return (
    <Box sx={{ backgroundColor: "#1e222a", padding: "16px", borderRadius: "8px" }}>
      <Typography variant="body1">Settings Content Here...</Typography>
    </Box>
  );
};

export default SettingsTab;
